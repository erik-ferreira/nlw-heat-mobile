import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    paddingHorizontal: 20,
  },

  contentList: {
    paddingTop: 135,
    paddingBottom: 184,
  },
});

export { styles };
